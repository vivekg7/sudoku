name: cli
description: Sudoku CLI — play sudoku in the terminal.
version: 0.0.1
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.11.1

# Add regular dependencies here.
dependencies:
  args: ^2.7.0
  sudoku_core:
    path: ../../packages/sudoku_core

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
