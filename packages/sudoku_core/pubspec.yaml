name: sudoku_core
description: Core Sudoku logic — models, solver, generator, hints.
version: 1.3.0
repository: https://github.com/vivekg7/sudoku

environment:
  sdk: ^3.11.1

# Add regular dependencies here.
dependencies:
  # path: ^1.9.0

dev_dependencies:
  lints: ^6.1.0
  test: ^1.30.0
